spring.application.name=backend
spring.jpa.hibernate.ddl-auto=update

spring.datasource.url=jdbc:mysql://localhost:3306/inventory_management
spring.datasource.username=root
spring.datasource.password=Myroot123#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

server.port=8080

spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
google.recaptcha.secret=6LcnPCYrAAAAADHUFru7nDFQuWcxHsNH60BCk7pY


# File upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# CORS settings
spring.web.cors.allowed-origins=http://localhost:3000,http://localhost:3001
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# Disable Spring Security autoconfiguration to use our custom config
spring.security.filter.dispatcher-types=REQUEST

# Set the root logging level to INFO
logging.level.root=INFO

# Set specific logging levels for packages
logging.level.org.springframework.web=DEBUG
logging.level.com.yourcompany=TRACE

# Specify the log file location
logging.file.name=logs/application.log

# Set the log pattern (optional)
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

